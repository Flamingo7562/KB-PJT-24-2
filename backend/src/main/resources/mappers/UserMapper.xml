<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gighub.member.mapper.UserMapper">

    <select id="countByLoginId" resultType="int">
        SELECT COUNT(*) FROM users WHERE login_id = #{loginId}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="com.gighub.member.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (login_id, email, password_hash, name, phone, role, status)
        VALUES (#{loginId}, #{email}, #{passwordHash}, #{name}, #{phone}, #{role}, 'ACTIVE')
    </insert>

</mapper>
