<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gighub.bank.mapper.MockBankMapper">

    <resultMap id="mockBankAccountResult" type="com.gighub.bank.dto.MockBankAccount">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="bank_code" javaType="java.lang.String"/>
            <arg column="mock_account_number" javaType="java.lang.String"/>
            <arg column="pin" javaType="java.lang.String"/>
            <arg column="balance" javaType="java.lang.Long"/>
            <arg column="available_amount" javaType="java.lang.Long"/>
            <arg column="status" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <!-- camelCase 매핑에 의존하지 않도록 컬럼을 명시한다 -->
    <sql id="accountColumns">
        id, bank_code, mock_account_number, pin, balance, available_amount, status
    </sql>

    <!-- 비귀속 Fixture 식별. status와 무관하게 존재 자체만 조회한다(Idempotency Replay가
         현재 상태와 무관하게 원래 계좌를 다시 찾을 수 있어야 한다). -->
    <select id="findAccountIdByBankCodeAndAccountNo" resultType="java.lang.Long">
        SELECT id
        FROM mock_bank_accounts
        WHERE bank_code = #{bankCode} AND mock_account_number = #{accountNo}
    </select>

    <select id="getAccountById" resultMap="mockBankAccountResult">
        SELECT <include refid="accountColumns"/>
        FROM mock_bank_accounts
        WHERE id = #{accountId}
    </select>

    <select id="getAccountForUpdate" resultMap="mockBankAccountResult">
        SELECT <include refid="accountColumns"/>
        FROM mock_bank_accounts
        WHERE id = #{accountId}
        FOR UPDATE
    </select>

    <!-- ck_mock_bank_accounts_amounts(available <= balance) 때문에 두 컬럼을 한 문장에서 갱신한다 -->
    <update id="withdrawFromAccount">
        UPDATE mock_bank_accounts
        SET balance          = balance - #{amount},
            available_amount = available_amount - #{amount},
            updated_at       = NOW(6)
        WHERE id = #{accountId}
          AND status = 'ACTIVE'
          AND available_amount &gt;= #{amount}
    </update>

    <update id="depositToAccount">
        UPDATE mock_bank_accounts
        SET balance          = balance + #{amount},
            available_amount = available_amount + #{amount},
            updated_at       = NOW(6)
        WHERE id = #{accountId}
          AND status = 'ACTIVE'
    </update>

    <!-- append-only. status는 SUCCESS로 확정 INSERT한다 -->
    <insert id="insertBankTransaction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mock_bank_transactions
        (account_id, bank_tran_id, transfer_type, amount,
         balance_before, balance_after, reference_type, reference_id, status)
        VALUES
            (#{accountId}, #{bankTranId}, #{transferType}, #{amount},
             #{balanceBefore}, #{balanceAfter}, #{referenceType}, #{referenceId}, 'SUCCESS')
    </insert>

</mapper>
