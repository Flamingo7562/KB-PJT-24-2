<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gighub.wallet.mapper.FundingMapper">

    <!-- DTO가 setter를 갖지 않으므로 생성자 인자로 매핑한다. 인자 순서는 필드 선언 순서를 따른다 -->
    <resultMap id="fundingOrderResult" type="com.gighub.wallet.dto.FundingOrder">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="employerId" javaType="java.lang.Long"/>
            <arg column="linkedAccountId" javaType="java.lang.Long"/>
            <arg column="expectedAmount" javaType="java.lang.Long"/>
            <arg column="transferredAmount" javaType="java.lang.Long"/>
            <arg column="mockBankTransactionId" javaType="java.lang.Long"/>
            <arg column="status" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <select id="findByIdempotencyKeyForShare"
            resultMap="fundingOrderResult">
        SELECT id                       AS id,
               employer_id              AS employerId,
               linked_account_id        AS linkedAccountId,
               expected_amount          AS expectedAmount,
               transferred_amount       AS transferredAmount,
               mock_bank_transaction_id AS mockBankTransactionId,
               status                   AS status
        FROM funding_orders
        WHERE idempotency_key = #{idempotencyKey}
        FOR SHARE
    </select>

    <insert id="insertFundingOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO funding_orders
        (employer_id, linked_account_id, expected_amount, idempotency_key, status)
        VALUES
        (#{employerId}, #{linkedAccountId}, #{expectedAmount}, #{idempotencyKey}, 'READY')
    </insert>

    <update id="completeFundingOrder">
        UPDATE funding_orders
        SET transferred_amount       = #{transferredAmount},
        mock_bank_transaction_id = #{bankTransactionId},
        status                   = 'COMPLETED',
        completed_at             = NOW(6),
        updated_at               = NOW(6)
        WHERE id = #{id} AND status = 'READY'
    </update>

</mapper>
