/**
 * 여러 도메인이 함께 사용하는 API 경계와 요청 추적 기능을 제공합니다.
 *
 * <p>이 패키지는 모든 도메인이 의존할 수 있는 최소한의 공통 계약만 담당합니다. 지갑,
 * 은행, 문서처럼 특정 도메인에서만 사용하는 비즈니스 규칙과 상태 판단은 각 도메인
 * 패키지에 남겨 둡니다.</p>
 *
 * <h2>하위 패키지</h2>
 * <ul>
 *   <li>{@code api}: 외부에 공개하는 오류 Code와 JSON 응답 모델</li>
 *   <li>{@code exception}: 도메인 예외를 승인된 오류 응답으로 변환하는 공통 계약</li>
 *   <li>{@code trace}: 오류 응답과 서버 로그를 연결하는 요청 추적 식별자</li>
 * </ul>
 *
 * <p>공통 계층이 구체적인 도메인 패키지를 import하지 않는 방향으로 의존성을 유지합니다.</p>
 */
package com.gighub.common;

