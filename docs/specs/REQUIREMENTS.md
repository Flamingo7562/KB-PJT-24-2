# 요구사항 정의서

| 항목        | 값              |
| ----------- | --------------- |
| 명세 릴리스 | `2.1.1`         |
| 승인일      | 2026-08-05      |
| 소유자      | PM/Admin Master |

이 문서는 Gig Hub가 제공해야 하는 제품 기능과 수용 기준을 정의합니다. HTTP 계약은
[API_SPEC.md](API_SPEC.md), 미결 정책은 [DECISIONS.md](DECISIONS.md), 요구사항별 연결은
[SPEC_TRACEABILITY.md](SPEC_TRACEABILITY.md)를 따릅니다.

## 읽는 법

- 요구사항 ID는 기획, API, 도메인, 테스트를 연결하는 안정적인 식별자입니다.
- 우선순위는 `P0`(핵심), `P1`(중요), `P2`(후순위), `Future`(후속 제품 범위)입니다.
- 수용 기준은 기능의 완료 조건입니다. 화면이나 저장 구조의 존재만으로 충족되지 않습니다.
- Open 결정이 연결된 항목은 승인된 범위까지만 계약이며, 미결 부분을 임의로 보완하지
  않습니다.

## 공통 제품 원칙

- 가입과 로그인은 `OWNER`, `WORKER` 역할 흐름을 구분합니다.
- 인증은 Spring Security 기반 HttpSession을 사용하며 역할과 리소스 소유권, CSRF를 서버가
  검증합니다.
- 입력은 승인된 정규화 후 검증합니다. 전화번호는 공백·하이픈을 제거한 숫자 문자열로
  저장·반환하고 비밀번호 원문은 trim하거나 대소문자를 바꾸지 않습니다.
- 전화번호는 서로 독립된 `users.phone`, `workplaces.phone`에만 정규화된 평문으로 저장하고
  요청·응답·SQL 바인딩 로그에 남기지 않습니다. 실제 개인정보 투입, 외부 공개 배포,
  검색·중복 검사 전에는 인증된 암호화를 재검토합니다.
- 일반 성공은 `{data: ...}`, 목록은 `{data: {content, page}}`, 오류는
  `{code, message, traceId, fieldErrors?}` 형식을 사용합니다.
- 목록은 `page=0`, `size=20`, 최대 `size=100`을 공통 기준으로 사용합니다.
- API에서 실제 발생 시점은 UTC `Instant`, 날짜 자체는 `LocalDate`로 표현합니다. 사용자가
  분리 입력한 날짜와 시간은 서버가 `Asia/Seoul` 기준으로 결합합니다.
- DB `DATETIME(6)`에는 `Asia/Seoul` 지역 시각을 저장하고 API 경계에서 UTC로 변환합니다.
- 금액은 음수가 아닌 KRW 원 단위 정수입니다.
- 충전, 출금, 초대 수락, 정산 승인은 멱등성과 원자성을 보장합니다.
- 파일 본문은 외부 비공개 저장소에 두고 DB에는 식별 Key, Checksum, Version과 감사 정보를
  보관합니다.

## 회원·인증·신뢰 뱃지

| ID        | 우선순위 | 제품 요구                                                                        | 수용 기준                                                                                                                                                                                                                                                                                 |
| --------- | -------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUTH-001  | P0       | 첫 진입에서 OWNER와 WORKER 흐름을 선택한다.                                      | 선택 역할을 가입·로그인에 전달하고, 인증 사용자의 역할이 다르면 해당 역할 화면과 API 접근을 거부한다.                                                                                                                                                                                     |
| AUTH-002  | P0       | 아이디, 비밀번호와 확인값, 이름, 이메일, 선택 전화번호로 역할별 가입을 제공한다. | `users`와 KRW `wallets`만 원자적으로 만들고 비밀번호는 단방향 Hash만 저장한다. OWNER도 별도 Profile을 만들지 않으며 사업장 등록은 가입 다음 단계로 분리한다. 정규화한 아이디와 이메일은 비어 있지 않고 각각 최대 50자·255자여야 하며, 비밀번호는 8~64자이면서 UTF-8 72byte 이하여야 한다. |
| AUTH-003  | P0       | 아이디와 이메일 사용 가능 여부를 각각 확인한다.                                  | 아이디·이메일을 trim 후 소문자로 정규화하고 비어 있지 않은 최대 50자·255자 값으로 검증하며, 가용성 조회와 가입에 같은 규칙을 적용한다. 사전 확인과 무관하게 최종 가입에서 중복을 다시 확인하고 `409 CONFLICT`로 응답한다.                                                                 |
| AUTH-004  | P0       | 로그인, 로그아웃, 인증 복원과 역할·사업장 보유 여부를 제공한다.                  | 비인증 Session 조회는 `200 authenticated=false`다. ACTIVE 사용자만 로그인하고 Session ID를 교체한다. 로그인 실패 사유는 구분하지 않고 `401 AUTH_REQUIRED`로 응답한다.                                                                                                                     |
| AUTH-005  | P0       | 활성 사업장이 없는 OWNER를 첫 사업장 등록으로 안내한다.                          | 로그인과 Session 조회 때마다 OWNER의 ACTIVE 사업장 수를 DB에서 계산하며 0개일 때만 `needsWorkplaceSetup=true`다. 결과를 Session에 저장하지 않는다.                                                                                                                                        |
| AUTH-006  | P0       | Session 생성, 만료, 재로그인과 로그아웃 수명주기를 관리한다.                     | 앱 최초 실행·로그인·로그아웃 성공 뒤 204 CSRF 준비 API를 호출하고 로그인·로그아웃 POST도 CSRF를 검증한다. 실패는 `403 FORBIDDEN`이며 상태 변경 요청을 자동 재실행하지 않는다.                                                                                                             |
| AUTH-007  | P0       | 역할과 리소스 소유관계로 화면과 API 접근을 나눈다.                               | URL이나 요청 본문의 사용자 ID를 권한 근거로 신뢰하지 않고 인증 주체를 서버에서 대조한다.                                                                                                                                                                                                  |
| AUTH-008  | P0       | 내 정보를 조회하고 전화번호만 변경한다.                                          | 가입 후 `loginId`, `email`, `name`은 변경할 수 없다. 전화번호는 정규화 후 검증해 `users.phone`에 숫자만 저장·반환하며 `role`, `status`를 포함한 금지 필드는 검증 오류로 거부한다.                                                                                                         |
| AUTH-009  | P1       | 현재 비밀번호 확인 후 새 비밀번호로 변경한다.                                    | 현재 비밀번호가 일치하고 새 비밀번호가 8~64자이면서 UTF-8 72byte 이하일 때만 변경한다. 입력을 절단하지 않고 평문 비밀번호를 응답, 로그, DB에 남기지 않는다.                                                                                                                               |
| AUTH-010  | P1       | 사용자를 물리 삭제하지 않고 탈퇴 처리한다.                                       | 잔액, 예치금 또는 진행 중 근무가 있으면 탈퇴를 거부하고 탈퇴 후 로그인할 수 없다.                                                                                                                                                                                                         |
| AUTH-011  | P1       | 등록 이메일을 식별값으로 일회성 비밀번호 재설정을 제공한다.                      | 계정 존재 여부를 노출하지 않는다. Token은 만료되고 한 번만 사용할 수 있으며 DB에는 Hash만 저장한다. 새 비밀번호는 공통 경계를 적용하고 원문 전달 채널은 `DEC-OPEN-PASSWORD-RESET-DELIVERY`를 따른다.                                                                                      |
| BADGE-001 | P2       | 역할별 이력으로 신뢰 뱃지의 최신 등급과 근거를 산정한다.                         | 최근 5·10·15건 기준을 적용하고 재검증 가능한 근거를 보존한다.                                                                                                                                                                                                                             |
| BADGE-002 | P2       | 내 최신 뱃지와 다음 등급까지 남은 조건을 표시한다.                               | 미부여 상태를 구분하고 저장된 근거와 원천 이력의 계산 결과가 일치한다.                                                                                                                                                                                                                    |
| BADGE-003 | P2       | 초대 Link 로그인 후 초대한 OWNER의 뱃지를 표시한다.                              | 비로그인 접근은 WORKER 로그인으로 보내며 원래 초대 경로를 보존한다. 로그인 후 Token을 다시 검증한 뒤 OWNER 뱃지를 노출하고, 인증 전에는 초대 내용과 뱃지를 노출하지 않는다.                                                                                                               |

## 사업장·은행 계좌·지갑

| ID            | 우선순위 | 제품 요구                                                                                  | 수용 기준                                                                                                                                                                                             |
| ------------- | -------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WORKPLACE-001 | P0       | OWNER가 사업자등록번호, 상호, 대표자, 주소, 전화번호와 선택 좌표로 복수 사업장을 등록한다. | 사업체·사업장 기준정보는 `workplaces`에만 저장한다. 사업자등록번호는 숫자 10자리이며 중복될 수 없다. 전화번호는 정규화한 숫자만 저장하고 좌표는 함께 입력하거나 모두 생략하며 반경은 항상 100m다.     |
| WORKPLACE-002 | P1       | OWNER는 소유한 활성 사업장의 상호, 주소, 전화번호만 수정한다.                              | 전화번호는 정규화한 숫자를 `workplaces.phone`에 저장한다. 대표자, 좌표, 인증 반경은 직접 수정할 수 없고 금지 필드는 검증 오류로 거부한다. 주소 변경 규칙은 `DEC-OPEN-WORKPLACE-COORDINATES`를 따른다. |
| WORKPLACE-003 | P1       | 진행 중이거나 미래인 근무가 없는 사업장을 논리 비활성화한다.                               | 비활성 사업장은 전역 Context와 신규 근무 등록에서 제외하지만 OWNER 관리 목록에는 남긴다. `DELETED` 사업장은 목록에서 제외하고 과거 근무·계약 Snapshot은 유지한다.                                     |
| WORKPLACE-004 | P0       | OWNER가 활성 사업장을 전역 작업 Context로 선택한다.                                        | OWNER 관리 목록은 소유한 `ACTIVE`·`INACTIVE`를 반환하고 `DELETED`를 제외한다. Context는 `ACTIVE`만 선택하며 소유권을 매 요청에서 확인한다. 지갑 요약은 사용자 전체 기준이다.                          |
| BANK-001      | P0       | 사용자가 은행과 계좌번호를 직접 입력하고 서버가 본인 소유 계좌를 식별한다.                 | 숫자 3자리 은행 코드와 숫자 계좌번호를 정규화하고 인증 사용자 소유의 ACTIVE 계좌만 찾는다. 다른 사용자의 계좌 존재 여부는 노출하지 않는다.                                                            |
| WALLET-001    | P0       | 가용 잔액과 예치 금액을 분리해 표시한다.                                                   | 대표 잔액은 가용 잔액이며 예치 금액을 중복 합산하지 않는다.                                                                                                                                           |
| WALLET-002    | P0       | OWNER가 본인 계좌에서 지갑으로 충전한다.                                                   | `{bankCode, accountNo, amount}`로 계좌를 식별한다. 계좌 차감, 은행 원장, 주문 완료, 지갑 증가와 지갑 원장을 한 트랜잭션으로 처리하고 성공 후 지갑을 재조회한다.                                       |
| WALLET-003    | P0       | 사용자가 본인 계좌로 가용 잔액을 출금한다.                                                 | `{bankCode, accountNo, amount}`로 계좌를 식별한다. 출금 요청, 지갑 감소, 계좌 입금과 양쪽 원장을 원자적으로 처리하고 성공 후 잔액을 재조회한다.                                                       |
| WALLET-004    | P0       | 기간, 사업장, 거래 유형, 금액, 검색어와 정렬 조건으로 지갑 거래를 조회한다.                | `from`, `to`, `type`과 공통 페이지 계약을 사용하며 거래 생성 시점을 UTC `Instant`로 반환한다.                                                                                                         |
| WALLET-005    | P0       | 모든 지갑·은행 금액 변경을 수정 불가 원장으로 남긴다.                                      | 금액 변경과 원장 추가가 같은 트랜잭션에서 성공하며 일반 기능은 기존 원장을 수정하거나 삭제하지 않는다.                                                                                                |
| WALLET-006    | P0       | 충전, 출금, 초대 수락과 정산 승인의 중복·동시 요청을 한 번만 반영한다.                     | 같은 멱등 Key와 같은 요청은 같은 결과를 반환하고 다른 요청에 같은 Key를 사용하면 거부한다. 잠금 순서와 전체 Rollback을 일관되게 적용한다.                                                             |

## 홈·근무·초대·계약

| ID           | 우선순위 | 제품 요구                                                                         | 수용 기준                                                                                                                                                                                                                                                                            |
| ------------ | -------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DASH-001     | P0       | WORKER 홈에 오늘의 근무와 상태를 표시한다.                                        | `Asia/Seoul` 당일 근무와 성공 출퇴근 기록을 조합한다. 지각은 별도 저장 상태가 아닌 파생 표시다.                                                                                                                                                                                      |
| DASH-002     | P1       | 근무 경과에 따른 확보 안심금액을 화면 계산값으로 표시한다.                        | 화면 진입 즉시 계산하고 근무 종료 전까지 60초마다 갱신한다. 값은 0원 이상 합의 일급 이하이며 지갑·에스크로 원금은 바꾸지 않는다. 무급 휴게 반영 규칙은 `DEC-OPEN-DASHBOARD-BREAK`를 따른다.                                                                                          |
| DASH-003     | P2       | 일용근로소득 원천징수 기준의 예상 실수령액을 참고값으로 표시한다.                 | `과세표준=max(일당-150,000원, 0원)`, `소득세=과세표준×2.7%`, `지방소득세=과세표준×0.27%`로 계산한 뒤 각 세액의 10원 미만을 버린다. 절사한 소득세가 1,000원 미만이면 두 세액을 모두 0원으로 한다. `예상 실수령액=일당-소득세-지방소득세`이며 실제 MVP 정산 원금은 합의 일급 전액이다. |
| WORK-001     | P0       | 선택 사업장의 근무 건수를 상태별로 요약한다.                                      | `DRAFT`, `ACCEPTED`, `READY`, `IN_PROGRESS`, `CHECK_OUT_MISSING`, `COMPLETED`, `NO_SHOW`, `CANCELED`를 서로 구분한다.                                                                                                                                                                |
| WORK-002     | P0       | 사업장 근무 목록을 날짜와 시작 시각 기준으로 조회·검색한다.                       | 소유권, 페이지네이션과 정렬을 서버에서 적용하고 미매칭 근로자는 `null`로 반환한다.                                                                                                                                                                                                   |
| WORK-003     | P0       | 제목, 일시, 휴게, 일급으로 `DRAFT` 근무를 등록한다.                               | 시간과 금액을 검증하고 사업장 이름, 주소, 좌표와 100m 반경을 근무 조건 Snapshot에 남긴다.                                                                                                                                                                                            |
| WORK-004     | P0       | 근무 조건, 상태, 초대, 계약, 근태, 정산과 매칭 정보를 상세 조회한다.              | 당사자와 소유권을 확인하며 민감 문서는 별도 문서 권한 검사를 거친다.                                                                                                                                                                                                                 |
| WORK-005     | P0       | 계약 전 근무 조건만 수정하고 조건 Version을 증가시킨다.                           | `ACCEPTED` 이후에는 수정하지 않고 조건 변경 시 해당 이전 조건의 Pending 초대를 철회한다.                                                                                                                                                                                             |
| WORK-006     | P0       | 이력이 없는 `DRAFT`는 삭제하고 초대 이력이 있으면 취소 상태로 전환한다.           | 계약과 에스크로가 생성된 근무의 임의 취소는 허용하지 않고 참조 무결성 오류를 제품 오류로 변환한다.                                                                                                                                                                                   |
| WORK-007     | P0       | WORKER가 확정, 진행, 퇴근 확인 필요, 완료와 노쇼 근무 이력을 조회한다.            | 인증 WORKER의 근무만 반환하고 `CHECK_OUT_MISSING`은 “퇴근 확인 필요”로 표시하며 에스크로·정산 상태를 함께 제공한다.                                                                                                                                                                  |
| INVITE-001   | P0       | 미매칭 근무에 만료형 초대 Link를 발급한다.                                        | Token 원문 대신 Hash를 저장하고 근무·조건 Version별 활성 Pending 초대를 하나만 유지한다.                                                                                                                                                                                             |
| INVITE-002   | P0       | 초대 Link 접근 시 로그인 후 원래 근무 확정 화면으로 복귀한다.                     | Redirect를 보존하고 로그인 후 Token 상태, 만료, 조건 Version과 대상 WORKER를 다시 검증한다.                                                                                                                                                                                          |
| INVITE-003   | P0       | 초대 대상 근무 조건과 초대한 OWNER의 뱃지를 읽기 전용으로 표시한다.               | 조건 Version이 다르거나 초대가 만료·철회·수락 완료 상태이면 확정을 막는다.                                                                                                                                                                                                           |
| CONTRACT-001 | P0       | WORKER가 조건과 취소 제한을 확인하고 Body 없는 수락 요청으로 최종 동의한다.       | 요청 자체를 수락 의사로 기록하고 사용자, 근무, 금액 또는 전자서명 원본을 Body로 받지 않는다.                                                                                                                                                                                         |
| CONTRACT-002 | P0       | 초대 수락 시 매칭, 계약 Snapshot, 일급 HOLD와 정산 예약을 한 트랜잭션으로 만든다. | 서버가 Token으로 당사자, 근무와 금액을 도출한다. 가용 잔액이 부족하면 전체 실패하고 모든 도메인의 당사자와 금액이 일치한다.                                                                                                                                                          |
| CONTRACT-003 | P0       | 계약 확정 과정에서 서버가 근로계약서를 자동 생성한다.                             | 근무에 연결된 최종 계약서를 양측이 동일하게 조회한다. 모든 Version은 시스템만 생성하며 사용자의 직접 업로드와 스캔 교체를 허용하지 않는다.                                                                                                                                           |

## 근태·정산·신고

| ID          | 우선순위 | 제품 요구                                                                      | 수용 기준                                                                                                                                                                                                       |
| ----------- | -------- | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ATT-001     | P0       | 사업장별로 만료되지 않는 활성 고정 QR을 하나 제공하고 OWNER가 재발급한다.      | 공개 nonce와 외부 HMAC Key로 Token을 검증한다. 재발급 즉시 기존 QR을 `REVOKED`로 만들고 사업장당 활성 QR을 하나만 유지한다. 재시도 계약은 `DEC-OPEN-QR-REISSUE-IDEMPOTENCY`를 따른다.                           |
| ATT-002     | P0       | WORKER가 같은 사업장 QR을 스캔하면 서버가 출근 또는 퇴근을 자동 판별한다.      | 해당 사업장에서 인증 WORKER의 처리 대상 근무는 최대 하나다. 성공 출근이 없으면 `CHECK_IN`, 출근만 있으면 `CHECK_OUT`, 둘 다 있으면 중복으로 거부한다.                                                           |
| ATT-003     | P0       | QR 스캔 위치를 사업장 좌표와 고정 100m 반경으로 검증한다.                      | 서버가 거리를 계산하고 100m 밖 시도를 거부 사유와 함께 기록한다. 클라이언트는 반경을 선택하거나 덮어쓸 수 없다.                                                                                                 |
| ATT-004     | P0       | 출퇴근 시도와 결과를 저장하고 조기 퇴근은 확인 후 기록한다.                    | 출근은 `READY→IN_PROGRESS`, 정상 퇴근은 `IN_PROGRESS→COMPLETED`로 전이한다. 예정 종료 전 두 번째 스캔은 기록하지 않고 확인을 요구하며 확인 재요청에 시점을 남긴다.                                              |
| ATT-005     | P0       | 성공 출근이 끝까지 없는 근무만 노쇼로 처리한다.                                | 성공 `CHECK_IN`이 없는 배정 근무만 `NO_SHOW` 후보가 된다. 성공 출근이 있으면 퇴근 기록이 없어도 노쇼로 바꾸지 않는다.                                                                                           |
| ATT-006     | P0       | 성공 출근 후 퇴근 기록이 없는 근무를 `CHECK_OUT_MISSING`으로 구분한다.         | 배정 WORKER와 성공 `CHECK_IN`이 있고 성공 `CHECK_OUT`이 없는 근무만 해당 상태가 될 수 있다. `NO_SHOW`와는 상호 배타적이다. 판정 시점, 실행 주체, 해소와 정산 흐름은 `DEC-OPEN-CHECK-OUT-MISSING-FLOW`를 따른다. |
| SETTLE-001  | P0       | 정상 퇴근 완료 후 유예 시간을 적용해 정산 예정 시각을 정한다.                  | `COMPLETED` 근무만 지급 예약 대상이며 퇴근 누락 상태는 `DEC-OPEN-CHECK-OUT-MISSING-FLOW`가 확정한 규칙을 따른다. 신고는 예정 시각을 바꾸지 않는다.                                                              |
| SETTLE-002  | P1       | 유예 시간 안에 해당 OWNER가 즉시 지급을 승인한다.                              | 근무 OWNER와 인증 사용자를 대조하고 중복 승인을 한 번만 반영한다.                                                                                                                                               |
| SETTLE-003  | P0       | 예정 시각이 지난 정산을 자동 지급한다.                                         | 여러 실행 주체가 동시에 처리해도 한 번만 지급하며 실패와 재시도 근거를 남긴다.                                                                                                                                  |
| SETTLE-004  | P0       | 에스크로 해제, OWNER 잠금 감소, WORKER 입금과 양측 원장을 원자적으로 처리한다. | 지급액은 합의 일급이며 완료된 정산을 다시 반영하지 않는다.                                                                                                                                                      |
| SETTLE-005  | P0       | 노쇼 시 예치금을 OWNER에게 환불하고 WORKER에게 지급하지 않는다.                | 성공 출근이 전혀 없는 `NO_SHOW`에만 적용하고 `ESCROW_REFUND` 원장을 한 번만 남긴다. 정산 종료 상태는 `DEC-OPEN-NO-SHOW-SETTLEMENT`를 따른다.                                                                    |
| CONTACT-001 | P2       | 근무 당사자인 WORKER에게 사업장 공개 연락처를 안내한다.                        | 해당 근무의 당사자만 조회하며 전화와 문자 연결을 제공한다.                                                                                                                                                      |
| DISPUTE-001 | P2       | 근무 당사자가 임금분쟁 제목과 경위를 제출한다.                                 | 당사자만 생성할 수 있고 신고 자체가 정산 상태나 예정 시각을 바꾸지 않는다.                                                                                                                                      |
| DISPUTE-002 | P2       | 신고자와 해당 근무 OWNER가 신고 내용, 상태와 처리 결과를 조회한다.             | 비당사자는 접근할 수 없고 해결 시각과 결과를 제공한다.                                                                                                                                                          |
| DISPUTE-003 | P2       | 분쟁 상태와 처리 이력을 관리한다.                                              | `OPEN`, `UNDER_REVIEW`, `RESOLVED`, `REJECTED`, `CANCELED`를 구분하고 열린 중복 신고를 제한한다. 관리자 처리 권한은 `DEC-OPEN-ADMIN-DISPUTE`를 따른다.                                                          |

## 문서

| ID      | 우선순위 | 제품 요구                                                                                          | 수용 기준                                                                                                                                                                                                                               |
| ------- | -------- | -------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DOC-001 | P1       | OWNER가 소유 사업장의 계약서와 유효하게 공유받은 보건증을 조회한다.                                | 삭제·철회·만료된 항목은 기본 목록에서 제외하고 사업장 소유권을 확인한다.                                                                                                                                                                |
| DOC-002 | P1       | 초대 수락과 계약 확정 과정에서 근로계약서를 자동 생성한다.                                         | 계약서는 근무에 반드시 연결되고 모든 Version을 시스템만 생성한다. 근로계약서 직접 업로드와 스캔 파일 교체 기능은 제공하지 않는다.                                                                                                       |
| DOC-003 | P1       | OWNER가 공유자, 발급일, 만료일과 최신 보건증을 읽기 전용으로 본다.                                 | 유효한 활성 공유만 허용하고 수정·삭제를 막으며 열람과 다운로드를 감사한다.                                                                                                                                                              |
| DOC-004 | P1       | WORKER가 본인 근무 계약서의 최종 확정 Version을 조회·다운로드한다.                                 | 계약 당사자만 접근하고 양측이 같은 최종 Version을 본다. 전자서명 증거 보존은 `DEC-OPEN-E-SIGN-EVIDENCE`를 따른다.                                                                                                                       |
| DOC-005 | P1       | WORKER가 보건증 파일과 발급일을 등록한다.                                                          | 인증 WORKER가 소유자가 되며 보건증 유형만 허용하고 만료일을 서버가 검증한다.                                                                                                                                                            |
| DOC-006 | P1       | 보건증 발급일 수정, 파일 Version 추가와 논리 삭제를 제공한다.                                      | 삭제 시 활성 공유를 같은 트랜잭션에서 철회하고 기존 Version과 감사 기록을 보존한다.                                                                                                                                                     |
| DOC-007 | P1       | 확정됐고 시작 전인 근무의 사업장에 보건증을 공유한다.                                              | WORKER 사업장 목록에는 `ACTIVE`만 반환한다. 문서 소유권과 대상 근무를 확인하고 같은 목적의 활성 공유를 중복 생성하지 않는다.                                                                                                            |
| DOC-008 | P1       | 문서 소유자가 보건증 공유를 취소한다.                                                              | 즉시 상대 사업장의 접근을 거부하고 원본 논리 삭제 시 모든 활성 공유를 철회한다.                                                                                                                                                         |
| DOC-009 | P1       | 소유자, 계약 당사자 또는 유효 공유 대상만 최신 허용 Version을 열람·다운로드한다.                   | 매 요청에서 권한을 검증하고 저장소의 영구 공개 URL을 노출하지 않는다.                                                                                                                                                                   |
| DOC-010 | P1       | 보건증 업로드는 JPG, PNG, PDF와 정책 용량만 허용하고 계약서는 시스템이 PDF로 생성한다.             | 보건증은 확장자, MIME과 파일 Signature를 함께 확인한다. 사용자 업로드에서 계약서 유형을 거부한다.                                                                                                                                       |
| DOC-011 | P1       | 문서 Metadata, 열람과 다운로드의 허용·거부 결과를 감사한다.                                        | 사용자, 문서, Version, 동작, 결과, 시각과 거부 사유를 남기며 일반 기능에서 수정·삭제하지 않는다.                                                                                                                                        |
| DOC-012 | P1       | 자동 생성된 근로계약서는 사용자가 직접 삭제할 수 없으며 근로일 이후 3년간 보존한 뒤 자동 삭제한다. | 사용자 요청으로 계약서를 삭제하지 않는다. 근로일을 기준으로 3년 보존기간이 끝난 대상은 서버가 멱등하게 자동 삭제한다. 야간 근무의 기준일과 파일·Metadata·Checksum·감사 기록별 삭제 범위는 `DEC-OPEN-DOCUMENT-RETENTION-SCOPE`를 따른다. |

## 알림·공통·외부 연동

| ID         | 우선순위 | 제품 요구                                                          | 수용 기준                                                                                                                                                                                                                                                                                    |
| ---------- | -------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ALERT-001  | P2       | 사용자별 인앱 알림을 조회하고 읽음 처리한다.                       | 수신자만 조회·변경하고 읽음 시각과 공통 페이지 정보를 제공한다. 세부 계약은 `DEC-OPEN-NOTIFICATION-CONTRACT`를 따른다.                                                                                                                                                                       |
| ALERT-002  | P2       | 근무, 예치, 정산, 환불, 문서 공유와 분쟁 이벤트에서 알림을 만든다. | 동일 이벤트의 중복을 막고 도메인 트랜잭션이 실패하면 알림을 확정하지 않는다.                                                                                                                                                                                                                 |
| COMMON-001 | P0       | 성공, 목록, 오류 응답과 요청 검증을 일관되게 적용한다.             | 성공은 `{data}`, 목록은 `content/page`, 오류는 `code/message/traceId/fieldErrors?`를 사용한다. 예상 밖의 서버 오류는 안전한 메시지의 `500 INTERNAL_ERROR`로 응답하고 응답과 서버 로그에 같은 `traceId`를 사용하며 내부 예외 메시지·SQL·Stack Trace·Token 원문과 민감 정보를 노출하지 않는다. |
| COMMON-002 | P0       | 역할, 소유관계와 당사자 불변식을 서버가 검증한다.                  | OWNER, 사업장, WORKER, 계좌, 지갑과 요청 사용자의 관계를 상태 변경 전에 확인한다.                                                                                                                                                                                                            |
| COMMON-003 | P0       | 금융, 계약, 근태, 문서와 감사 이력을 정책 기간 동안 보존한다.      | 일반 도메인 행은 상태 또는 삭제 시각으로 비활성화한다. 근로계약서 자동 삭제는 DOC-012와 연결 결정의 범위만 예외로 적용한다.                                                                                                                                                                  |
| COMMON-004 | P0       | 금액, 시간과 페이지 기준을 일관되게 적용한다.                      | KRW 정수, UTC `Instant`, `LocalDate`, DB 서울 지역 시각과 `page=0`, `size=20`, 최대 100 규칙을 지킨다.                                                                                                                                                                                       |
| EXT-001    | Future   | 외부 결제 Provider를 통한 지갑 충전을 지원한다.                    | Provider 주문, 승인과 취소를 저장하고 서버 검증 전에 지갑을 증가시키지 않는다. Provider 범위는 `DEC-OPEN-PAYMENT-PROVIDER`를 따른다.                                                                                                                                                         |
| EXT-002    | Future   | 결제 조회와 서명 검증 Webhook으로 금액과 상태를 재검증한다.        | 중복, 역순과 재전송에도 한 번만 반영하고 원문, 검증과 처리 결과를 감사 가능하게 보존한다.                                                                                                                                                                                                    |
